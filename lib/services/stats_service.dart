class StatsService {
  Future<void> recordListeningMinutes(int minutes) async {
    // TODO: Persist listening time and aggregate stats.
  }

  Future<void> setMonthlyGoalInHours(int hours) async {
    // TODO: Save goal locally (default 20h).
  }
}
